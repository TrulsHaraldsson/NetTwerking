package d7024e
/*
import (
	"testing"
)

func TestBucketAddContact(t *testing.T) {
	var b = newBucket()

	if b.Len() != 0 {
		t.Error("Expected bucket to be of length 0, got length", b.Len())
	}

	var contact = NewContact(NewRandomKademliaID(), "127.0.0.1:10000")
	b.AddContact(contact)
	
	if b.Len() != 1 {
		t.Error("Expected bucket to be of lenth 1, got length", b.Len())
	}
}
*/
